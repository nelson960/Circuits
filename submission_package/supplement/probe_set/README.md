# Probe Set Metadata

The fixed probe set is used for route scoring, cross-seed head ranking, and several formation diagnostics.

Included:

- `probe_set_metadata.json`: split counts, seed, and coverage metadata.

The full probe rows are omitted from the compact supplement to keep the archive small. They can be regenerated from the benchmark metadata and CLI commands if needed.
